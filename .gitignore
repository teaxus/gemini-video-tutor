# ── 本地配置（含 API 密钥）—— 绝对不要提交 ──
config.yaml
*.local.yaml
*.local.json
scripts/config.sh        # legacy 配置文件（已弃用），保留忽略以防旧副本

# ── 分析输出产物 ──
output/
*_frames/
*.converted.mp4
*.compressed.mp4

# ── 多轮追问会话状态（含对话内容与本地视频路径）──
sessions/

# ── 系统 / 缓存文件 ──
.DS_Store
__pycache__/
*.pyc

# ── 注意：prompts/ 是 skill 的一部分，必须提交，不要忽略 ──
